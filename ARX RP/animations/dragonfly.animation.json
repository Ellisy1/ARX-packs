{
	"format_version": "1.8.0",
	"animations": {
		"animation.dragonfly.hover": {
			"loop": true,
			"bones": {
				"dragonfly": {
					"rotation": ["math.sin(query.anim_time * 3000) * 0.5", "math.sin(query.anim_time * 100) * 4", 0],
					"position": [0, "math.sin(query.anim_time * 3000) * 0.1", 0]
				},
				"wings_r": {
					"rotation": [0, 0, "-math.sin(query.anim_time * 3000) * (60 + math.sin(query.anim_time * 360) * 10)"]
				},
				"wings_l": {
					"rotation": [0, 0, "math.sin(query.anim_time * 3000) * (60 + math.sin(query.anim_time * 360) * 10)"]
				}
			}
		}
	}
}